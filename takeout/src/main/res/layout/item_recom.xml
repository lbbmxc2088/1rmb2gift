<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="150dp"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/iv_recom_img"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:scaleType="centerCrop" />

    <TextView
        android:id="@+id/tv_recom_name"
        style="@style/Default_Textview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/iv_recom_img"
        android:layout_centerHorizontal="true" />

    <ProgressBar
        android:id="@+id/progress_shop_status"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="6dp"
        android:layout_below="@+id/tv_recom_name"
        android:background="@drawable/shape_shop_progress_bg"
        android:max="100"
        android:progress="10"
        android:progressDrawable="@drawable/shape_progressbar_goods" />

</RelativeLayout>